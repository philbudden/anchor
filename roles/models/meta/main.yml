---
# Ollama models role metadata
galaxy_info:
  author: Local LLM Server Project
  description: Manages Ollama model lifecycle (pull, refresh, prune)
  license: MIT
  min_ansible_version: "2.17"
  platforms:
    - name: MacOSX
      versions:
        - all

dependencies:
  - role: ollama

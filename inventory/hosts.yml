---
# Inventory for local-llm-server provisioning.
# Defines target macOS host(s) and their group membership.

all:
  children:
    macmini:
      hosts:
        macmini_primary:
          # Host connection details
          ansible_host: 192.168.1.100  # Replace with actual IP or hostname
          ansible_user: automation      # Replace with actual SSH user
          # ansible_port: 22             # Uncomment if non-standard SSH port
          # ansible_ssh_private_key_file: ~/.ssh/id_ed25519  # Uncomment if using SSH key auth
          
          # Host-specific variables (optional)
          # Override group_vars/macmini.yml here if needed
